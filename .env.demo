STORAGE_PATH    = "__storage__"

FASTAPI_PORT    = 5678
FASTAPI_PATH    = "."

NEXT_PUBLIC_POCKETBASE_URL="http://127.0.0.1:8099"
NEXT_PUBLIC_ENABLE_DEMO_ACCOUNTS=False

# Auto ingestion interval in minutes (default: 60)
NEXT_AUTO_INGEST_INTERVAL=60

# FASTAPI_WORKER_URL_* to load worker nodes via variables

